version: "3.8"
services:
  postgre:
    container_name: "notes-app"
    image: postgres:14
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fullstackopen
    volumes:
      - ./db-data/:/var/lib/postgresql/data
